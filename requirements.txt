# Runtime deps; keep in sync with [project].dependencies in pyproject.toml.
# Install: pip install -r requirements.txt  OR  pip install -e .
# Data sources
praw>=7.7.0
requests>=2.31.0
ddgs>=9.0.0

# Sentiment (VADER — fast, no GPU)
vaderSentiment>=3.3.2

# Utilities
python-dotenv>=1.0.0
rich>=13.7.0
